# Pipeline is dynamically generated by nix.
# See `nix/ci.nix`.
steps:
- label: ":pipeline:"
  command: nix eval --json .#pipeline.x86_64-linux.default | buildkite-agent pipeline upload --no-interpolation
